# Browser-visible API base. "/api" uses the Vite development proxy below.
VITE_API_BASE_URL=/api

# Used only by Vite's development server; it is not exposed to browser code.
VITE_API_PROXY_TARGET=http://localhost:5000
